services:
  db:
    image: pgvector/pgvector:pg16
    container_name: ozar_db
    environment:
      POSTGRES_USER: ozar
      POSTGRES_PASSWORD: ozar_password
      POSTGRES_DB: ozar
    ports:
      - "5433:5432"
    volumes:
      - ozar_pgdata:/var/lib/postgresql/data

volumes:
  ozar_pgdata:
